import React from "react";

function Footer() {
  return (
    <footer>
      <p>
        &copy; {new Date().getFullYear()} <a href="#">Já</a>
      </p>
    </footer>
  );
}

export default Footer;
